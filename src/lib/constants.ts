/** Canvas works in a fixed coordinate space (PLAN.md §6); screen scale is
 *  applied via CSS transform so stroke widths stay crisp at any zoom. */
export const CANVAS_W = 1600;
export const CANVAS_H = 1200;
